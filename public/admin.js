let products=[];const $=s=>document.querySelector(s);const pass=()=>$('#pass').value.trim();const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const attr=v=>String(v??'').replace(/'/g,"\\'");async function api(url,options={}){const headers={...(options.headers||{}),'x-admin-password':pass()};const r=await fetch(url,{...options,headers});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||'حدث خطأ.');return d}async function refresh(){try{products=await api('/api/admin/products');render()}catch(e){$('#adminList').innerHTML=`<p style="color:#b21d1d">${esc(e.message)}</p>`}}function render(){$('#adminList').innerHTML=products.map(p=>`<div class="cart-row" style="align-items:center"><div style="flex:1"><b>${esc(p.name)}</b><br><small>${Number(p.price||0).toLocaleString('ar-EG')} ج.م — ${(p.images||[p.image]).filter(Boolean).length} صور — <strong style="color:${p.status==='sold'?'#b21d1d':'#16834b'}">${p.status==='sold'?'مباعة':'متاحة'}</strong></small></div><button class="btn" onclick="editProduct('${attr(p.id)}')">تعديل</button><button class="btn" onclick="delProduct('${attr(p.id)}')">حذف</button></div>`).join('')||'<p style="color:#777">لا توجد سيارات.</p>'}window.editProduct=id=>{const p=products.find(x=>x.id===id);if(!p)return;const f=$('#productForm');['id','name','price','year','category','transmission','fuel','badge','description','status'].forEach(k=>f.elements[k].value=p[k]??'');f.elements.images.value=(p.images||[p.image]).filter(Boolean).join('\n');$('#imagePreview').innerHTML=(p.images||[p.image]).filter(Boolean).map(u=>`<img src="${esc(u)}" style="width:80px;height:60px;object-fit:cover;border-radius:9px">`).join('');scrollTo({top:0,behavior:'smooth'})};window.delProduct=async id=>{if(!confirm('حذف السيارة نهائيًا؟'))return;try{await api('/api/admin/products/'+encodeURIComponent(id),{method:'DELETE'});await refresh()}catch(e){alert(e.message)}};$('#productForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/admin/products',{method:'POST',body:new FormData(e.target)});alert('تم حفظ السيارة بنجاح');e.target.reset();$('#imagePreview').innerHTML='';await refresh()}catch(err){alert(err.message)}};$('#pass').addEventListener('change',refresh);$('#imageFiles').addEventListener('change',async e=>{const files=[...e.target.files];if(!files.length)return;const status=$('#uploadStatus');const ta=$('#productForm').elements.images;const allowed=['image/jpeg','image/png','image/webp','image/gif'];if(files.some(f=>!allowed.includes(f.type))){status.textContent='مسموح فقط JPG وPNG وWEBP وGIF.';return}if(files.some(f=>f.size>2*1024*1024)){status.textContent='حجم كل صورة يجب ألا يتجاوز 2 ميجابايت.';return}const old=ta.value.split(/\r?\n+/).map(x=>x.trim()).filter(Boolean);let urls=[];try{for(let i=0;i<files.length;i++){status.textContent=`جاري رفع الصورة ${i+1} من ${files.length}...`;const fd=new FormData();fd.append('images',files[i]);const d=await api('/api/admin/upload-images',{method:'POST',body:fd});urls.push(...(d.urls||[]))}ta.value=[...old,...urls].join('\n');$('#imagePreview').innerHTML=ta.value.split(/\r?\n+/).filter(Boolean).map(u=>`<img src="${esc(u)}" style="width:80px;height:60px;object-fit:cover;border-radius:9px">`).join('');status.textContent=`تم رفع ${urls.length} صورة. اضغط حفظ السيارة.`;e.target.value=''}catch(err){status.textContent=err.message}});
